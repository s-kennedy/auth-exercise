<h1>Sign up!</h1>

<%= form_for :user, url: '/users' do |f| %>

	<%= f.label :username %>
	<%= f.text_field :username %>

	<%= f.label :email %>
    <%= f.text_field :email %>

    <%= f.submit "Submit" %>
    
<% end %>